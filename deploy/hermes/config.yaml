# Hermes Agent configuration for the Analytical Tools stack.
# Environment variables still take precedence for credentials.

model:
  default: "gemini-2.5-flash-lite"
  provider: "gemini"

terminal:
  backend: "local"
  cwd: "."
  timeout: 180
  docker_mount_cwd_to_workspace: false
  lifetime_seconds: 300
